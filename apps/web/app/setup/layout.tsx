import "../globals.css";

export const metadata = {
  title: "Setup - LeMedia",
  description: "First-time setup for LeMedia",
};

export default function SetupLayout({ children }: { children: React.ReactNode }) {
  return children;
}

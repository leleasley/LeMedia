import { UserLinkedAccountsClient } from "@/components/Settings/Users/UserLinkedAccountsClient";

export const metadata = {
    title: "Linked Accounts - User settings - LeMedia",
};

export default function UserLinkedAccountsPage() {
    return <UserLinkedAccountsClient />;
}
